% Chapter Template

% todo en la parte de datasets se me sugirió mover aquí esto...
% Algunos de los experimentos que no resultaron exitosos comprenden:
% pruebas con solo variables predictivas sin embeddings, demuestran que no es posible capturar eventos como el público en vivo o el habla en los primeros intentos de inferencia de \textit{liveness} y \textit{speechiness}. Las pruebas con embeddings pero sin variables predictivas en general no proporcionan la información de \textit{key}, \textit{tempo}, \textit{loudness}, \textit{mood} o \textit{genres}.
% Pruebas con distintas ventanas resultaron ser ineficientes en el proceso de extracción de más de 60~000 canciones. Pruebas aplicando todos los estadísticos resultaron en un espacio de variables que consume mucho espacio de almacenamiento y con alta correlación.

\chapter{Ensayos y resultados} % Main chapter title

\label{Chapter4} % Change X to a consecutive number; for referencing this chapter elsewhere, use \ref{ChapterX}
% los capítulos deben comenzar con un breve párrafo introductorio que indique cuál es el contenido que se encontrará al leerlo.  La redacción sobre el contenido de la memoria debe hacerse en presente y todo lo referido al proyecto en pasado, siempre de modo impersonal.

Este capítulo presenta los resultados de los modelos seleccionados para cada variable objetivo, su comparacion con un experimento previo, los resultados de su evaluación con métricas de inteligencia artificial y su desempeño en un entorno de producción.

\section{Resultados predictivos}
\label{sec:resultados_modelos}

\subsection{Acousticness, Danceability, Energy, Instrumentalness y Valence}
%Un Perceptrón Multicapa (MLP) superó a las regresiones estadísticas iniciales, modelando eficientemente características continuas como \textit{Acousticness} y \textit{Danceability}.

En un ensayo preliminar, se entrenó un modelo multisalida únicamente con variables DSP para predecir siete variables en total, con \textit{Liveness} y \textit{Speechiness}, que comparten el rango continuo de valores entre 0 y 1.
Con este enfoque se obtuvo un rendimiento insuficiente, incapaz de predecir valores altos de \textit{Liveness} y \textit{Speechiness}, pero con métricas aceptables para las 5 variables restantes. Así se identificó la necesidad de separar las dos variables en preprocesamiento y la extracción de variables semánticas específicas de voz y presencia de público. De aquí se llegó a PANNs inference.

El modelo definitivo se reestructuró para las 5 variables restantes y se incorporaron los embeddings y probabilidades de PANNs.
En el entrenamiento se observa que la tasa de aprendizaje se disminuyó dinámicamente desde el $1,00\times10^{-4}$ inicial hasta $1,00\times10^{-6}$, al responder a los estancamientos en la función de pérdida, tal como se esperaba en el diseño descrito en la sección de diseño de la red neuronal \ref{sec:mlp}.
Finalmente, el mecanismo de early stopping detuvo el proceso en la iteración 74, después de 15 épocas sin reducir el error de validación mínimo de 0,0058, alcanzado en la iteración 59.
La figura \ref{fig:fivep_learning_curves} ilustra las curvas de aprendizaje del modelo durante el entrenamiento. 

\begin{figure}[htpb]
    \centering
    \includegraphics[width=0.9\textwidth]{curva_fivep.png}
    \caption{Curvas de aprendizaje del modelo MLP}
    \label{fig:fivep_learning_curves}
\end{figure}

La evolución de la pérdida de Huber a la izquierda y el MAE a la derecha, demuestran una convergencia rápida y estable. En ambos casos, las curvas de validación y entrenamiento se cruzan y estabilizan alrededor de las iteraciones 12 y 20, respectivamente. A partir de este punto, la validación se mantiene constante mientras el error de entrenamiento presenta un descenso marginal y evidencian aprendizaje sin sobreajuste.

La tabla \ref{tab:comparativa_mlp} muestra cómo la inclusión de embeddings y probabilidades de PANNs inference y la separación de \textit{speechiness} y \textit{liveness} mejoró consistentemente todas las métricas, con mejor R² global de 0,8152.

\begin{table}[htpb]
    \centering
    \caption{Comparativa de métricas de los dos experimentos de la red neuronal MLP.}
    \label{tab:comparativa_mlp}
    \renewcommand{\arraystretch}{1.2}
    \small
    \begin{tabular}{@{}lcccccc@{}}
        \toprule
        \multirow{2}{*}{\textbf{Variable}} & \multicolumn{2}{c}{\textbf{R² ($\uparrow$)}} & \multicolumn{2}{c}{\textbf{MAE ($\downarrow$)}} & \multicolumn{2}{c}{\textbf{MSE ($\downarrow$)}} \\
        \cmidrule(lr){2-3} \cmidrule(lr){4-5} \cmidrule(lr){6-7}
        & \textbf{Inicial} & \textbf{Definitivo} & \textbf{Inicial} & \textbf{Definitivo} & \textbf{Inicial} & \textbf{Definitivo} \\
        \midrule
        \textit{Acousticness}     & 0,8178 & \textbf{0,8903} & 0,0950 & \textbf{0,0681} & 0,0171 & \textbf{0,0106} \\
        \textit{Danceability}     & 0,6042 & \textbf{0,7381} & 0,0780 & \textbf{0,0594} & 0,0108 & \textbf{0,0063} \\
        \textit{Energy}           & 0,7584 & \textbf{0,8626} & 0,0809 & \textbf{0,0581} & 0,0110 & \textbf{0,0062} \\
        \textit{Instrumentalness} & 0,6733 & \textbf{0,8308} & 0,1231 & \textbf{0,0671} & 0,0355 & \textbf{0,0188} \\
        \textit{Valence}          & 0,6523 & \textbf{0,7539} & 0,1172 & \textbf{0,0964} & 0,0221 & \textbf{0,0162} \\
        \textit{Liveness}         & 0,1570 & -- & -- & -- & -- & -- \\
        \textit{Speechiness}      & 0,2778 & -- & -- & -- & -- & -- \\
        \bottomrule
    \end{tabular}
\end{table}

La figura \ref{fig:real_vs_pred_variables} muestra los resultados de los valores reales de las etiquetas y las estimaciones del modelo multisalida definitivo. En estos gráficos de dispersión, la línea diagonal de referencia representa el escenario de predicción perfecta. La proximidad y densidad de los puntos respecto a esta recta ilustran visualmente el nivel de precisión, el control de la varianza en los extremos y la capacidad de generalización frente a datos no vistos.

\begin{figure}[htpb]
    \centering
    \begin{subfigure}[b]{0.19\textwidth}
        \centering
        \includegraphics[width=\textwidth]{acousticness_actual_vs_predicted.pdf}
        \caption*{\scriptsize Acousticness}
        \label{fig:rvp_acousticness}
    \end{subfigure}
    \hfill
    \begin{subfigure}[b]{0.19\textwidth}
        \centering
        \includegraphics[width=\textwidth]{danceability_actual_vs_predicted.pdf}
        \caption*{\scriptsize Danceability}
        \label{fig:rvp_danceability}
    \end{subfigure}
    \hfill
    \begin{subfigure}[b]{0.19\textwidth}
        \centering
        \includegraphics[width=\textwidth]{energy_actual_vs_predicted.pdf}
        \caption*{\scriptsize Energy}
        \label{fig:rvp_energy}
    \end{subfigure}
    \hfill
    \begin{subfigure}[b]{0.19\textwidth}
        \centering
        \includegraphics[width=\textwidth]{instrumentalness_actual_vs_predicted.pdf}
        \caption*{\scriptsize Instrumentalness}
        \label{fig:rvp_instrumentalness}
    \end{subfigure}
    \hfill
    \begin{subfigure}[b]{0.19\textwidth}
        \centering
        \includegraphics[width=\textwidth]{valence_actual_vs_predicted.pdf}
        \caption*{\scriptsize Valence}
        \label{fig:rvp_valence}
    \end{subfigure}
    
    \caption{Dispersión de valores reales vs. estimados para las cinco variables perceptibles con el modelo definitivo.}
    \label{fig:real_vs_pred_variables}
\end{figure}

Un análisis de importancia de variables predictivas muestra predominio de atributos globales y ritmo de la tabla \ref{tab:espacio_variables}. La figura \ref{fig:feature_importance_mlp} presenta las 15 variables más relevantes del modelo MLP. En la figura, el grado de importancia equivale a la caída en el coeficiente de determinación ($R^2$) que sufre el modelo al eliminar la información de dicha variable. Se destaca el impacto principal del Factor de cresta, representaciones chroma y estadísticos de onset.
También es interesante la ausencia de embeddings y probabilidades de PANNs en el top 15, pero muchas están presentes entre las variables seleccionadas (2 140 en total), lo que muestra que operan en conjunto

\begin{figure}[htpb]
    \centering
    \includegraphics[width=0.85\textwidth]{f_imp_fivep.png}
    \caption{Top-15 variables predictivas del modelo MLP.}
    \label{fig:feature_importance_mlp}
\end{figure}


\subsection{Loudness}

Para la estimación de la variable continua \textit{Loudness}, se implementó un modelo de regresión potenciado por gradiente mediante LightGBM. La optimización inicial utilizó muestreo unilateral basado en gradientes (GOSS) y una función objetivo basada en la penalización L1 (\texttt{regression\_l1}). La figura \ref{fig:loudness_learning_curves} expone las curvas de aprendizaje para esta configuración, donde se observa un descenso suave y continuo de los errores de entrenamiento y validación a medida que se suman árboles al modelo. La trayectoria estrictamente paralela de ambas curvas y su estabilización conjunta evidencian un aprendizaje estable.

\begin{figure}[htpb]
    \centering
    \includegraphics[width=0.85\textwidth]{curva_loudness_l1.png}
    \caption{Curvas de aprendizaje del modelo LightGBM para \textit{Loudness}.}
    \label{fig:loudness_learning_curves}
\end{figure}

Esta primera configuración alcanzó un coeficiente de determinación ($R^2$) de 0,8300, un RMSE de 1,8534 y un MAE de 1,1469. Para evaluar posibles mejoras en la predicción de valores atípicos, se entrenó una segunda configuración equivalente empleando regresión con penalización L2 (\texttt{regression\_l2}). Las métricas globales obtenidas fueron competitivas y ligeramente superiores en varianza explicada, logrando un $R^2$ de 0,8367, un RMSE de 1,8167 y un MAE de 1,1889.

A pesar de la similitud en el desempeño cuantitativo general, el modelo con penalización L2 se seleccionó como el definitivo debido a su superioridad para estimar valores en el extremo de la distribución, es decir audios con muy bajo volumen.
La figura \ref{fig:loudness_rvp_comparative} compara gráficamente el comportamiento de ambas configuraciones. A la izquierda, el modelo L1 presenta mayor dispersión en los valores inferiores, mientras que a la derecha, las estimaciones del modelo L2 se concentran con mayor precisión sobre la diagonal de predicción perfecta.

\begin{figure}[htpb]
    \centering
    \begin{subfigure}[b]{0.48\textwidth}
        \centering
        \includegraphics[width=\textwidth]{loudness_l1.png}
        \caption*{\scriptsize Penalización L1}
        \label{fig:loudness_l1}
    \end{subfigure}
    \hfill
    \begin{subfigure}[b]{0.48\textwidth}
        \centering
        \includegraphics[width=\textwidth]{loudness_l2.png}
        \caption*{\scriptsize Penalización L2 (Definitivo)}
        \label{fig:loudness_l2}
    \end{subfigure}
    
    \caption{Dispersión de valores actuales vs. estimados para \textit{Loudness}.}
    \label{fig:loudness_rvp_comparative}
\end{figure}

El análisis de importancia de variables, evaluado mediante la métrica de ganancia (\textit{gain}) en las divisiones de los árboles, confirma el impacto directo de los atributos globales. La figura \ref{fig:loudness_f_imp} detalla las 15 características predictivas principales.
El modelo prioriza fuertemente el Factor de cresta y los LUFS, seguidos de estadísticos de las representaciones DSP tradicionales como el RMS y el Centroide espectral. A diferencia de las variables perceptibles analizadas en la sección anterior, en este modelo sí destaca la presencia de un embedding de PANNs dentro del conjunto de mayor relevancia.

\begin{figure}[htpb]
    \centering
    \includegraphics[width=0.4\textwidth]{loudness_top15.png}
    \caption{Top-15 variables predictivas del modelo LightGBM para \textit{Loudness}.}
    \label{fig:loudness_f_imp}
\end{figure}

\subsection{Key y Mode}

Para la predicción conjunta de la tonalidad y el modo (\textit{Key} y \textit{Mode}), el problema se abordó como una clasificación multiclase de 24 categorías (12 tonalidades mayores y 12 menores). Se implementó un modelo LightGBM optimizado para la métrica de pérdida logarítmica multiclase (\texttt{multi\_logloss}). Durante el entrenamiento, la función de parada anticipada (\textit{early stopping}) detuvo el proceso en la iteración 736 al estabilizarse la pérdida de validación en 1,1926.

La evaluación global del modelo alcanzó una exactitud (\textit{accuracy}) de 0,6106 y un F1-Score ponderado de 0,6115. Destaca un AUC-ROC promedio de 0,9589, lo que indica una alta capacidad de discriminación entre las 24 clases. Adicionalmente, la exactitud Top-3 alcanzó un notable 0,8671, demostrando que la tonalidad correcta se encuentra casi siempre entre las tres primeras predicciones del sistema. 

La figura \ref{fig:key_confusion_matrix} presenta la matriz de confusión absoluta. El análisis de los errores revela una fuerte coherencia armónica: cuando el modelo se equivoca en la predicción principal con datos no vistos, las confusiones más frecuentes ocurren con la tonalidad relativa (mayor o menor correspondientes) o con las tonalidades dominante y subdominante.

\begin{figure}[htpb]
    \centering
    \includegraphics[width=1.0\textwidth]{key_cr.png}
    \caption{Matriz de confusión para la clasificación de 24 clases de \textit{Key} y \textit{Mode}.}
    \label{fig:key_confusion_matrix}
\end{figure}

Para contextualizar el desempeño, se compararon estos resultados con el extractor de tonalidad de la librería de estado del arte Essentia. La tabla \ref{tab:comparativa_essentia} demuestra que el modelo LightGBM entrenado supera holgadamente a la herramienta de referencia en todas las métricas globales, con una mejora cercana a los 18 puntos porcentuales en exactitud y 20 puntos en F1-Score ponderado.

La evaluación global del modelo alcanzó métricas muy superiores a los estándares tradicionales. Destaca un AUC-ROC ponderado de 0,9579, lo que indica una alta capacidad de discriminación entre las 24 clases. 

Para contextualizar este desempeño, la tabla \ref{tab:comparativa_essentia} contrasta los resultados del extractor de tonalidad de la librería Essentia con las predicciones exactas (\textit{Top-1}) y flexibles (\textit{Top-3}) del modelo LightGBM. El algoritmo entrenado supera holgadamente a la herramienta de referencia en todas las métricas. Adicionalmente, al evaluar el \textit{Top-3}, la exactitud y el F1-Score escalan por encima del 86~\%, demostrando que la tonalidad correcta se encuentra casi siempre entre las tres primeras opciones sugeridas por el sistema.

\begin{table}[htpb]
    \centering
    \caption{Comparativa de métricas globales entre el extractor de Essentia y las predicciones \textit{Top-1} y \textit{Top-3} del modelo LightGBM.}
    \label{tab:comparativa_essentia}
    \renewcommand{\arraystretch}{1.2}
    \small
    \begin{tabular}{@{}lccc@{}}
        \toprule
        \multirow{2}{*}{\textbf{Métrica}} & \multirow{2}{*}{\textbf{Essentia}} & \multicolumn{2}{c}{\textbf{Modelo LightGBM}} \\
        \cmidrule(l){3-4}
        & & \textbf{\textit{Top-1}} & \textbf{\textit{Top-3}} \\
        \midrule
        Exactitud (\textit{Accuracy})    & 0,4291 & 0,6106 & \textbf{0,8671} \\
        F1-Score (Macro)                 & 0,3780 & 0,5971 & \textbf{0,8589} \\
        F1-Score (Ponderado)             & 0,4076 & 0,6115 & \textbf{0,8672} \\
        AUC-ROC (Ponderado)              & --     & 0,9579 & \textbf{0,9579} \\
        \bottomrule
    \end{tabular}
\end{table}

Finalmente, el análisis de importancia de variables predictivas confirma la relevancia lógica de los atributos armónicos detallados previamente en el diseño de la solución. La figura \ref{fig:key_feature_importance} detalla que las decisiones del algoritmo dependen primariamente del \textit{Pitch}, las redes tonales (\textit{Tonnetz}), representaciones \textit{Chroma} y los perfiles de estimación tonal (como la fuerza tonal de Temperley). 

\begin{figure}[htpb]
    \centering
    \includegraphics[width=0.85\textwidth]{key_fi.png}
    \caption{Top-15 variables predictivas del modelo LightGBM para \textit{Key} y \textit{Mode}.}
    \label{fig:key_feature_importance}
\end{figure}

A nivel general, el análisis transversal de los tres modelos desarrollados (\textit{Perceptibles}, \textit{Loudness} y \textit{Key/Mode}) evidencia que las características DSP tradicionales y sus diversos estadísticos mantienen el dominio como las variables de mayor impacto predictivo, complementadas transversalmente por los \textit{embeddings} semánticos (PANNs) que operan en conjunto para refinar la generalización.

%El desbalanceo de clases se mitigó mediante pesos algorítmicos (\texttt{class\_weight='balanced'}), alcanzando un F1-Score macro de 0,5735 con LightGBM.

% \begin{figure}[htpb]
%     \centering
%     % \includegraphics[width=0.8\textwidth]{key_mode_confusion.png}
%     \caption{Matriz de confusión normalizada para \textit{Key / Mode}.}
%     \label{fig:key_mode_confusion}
% \end{figure}

\subsection{Tempo}

A diferencia del caso de \textit{Key} y \textit{Mode}, la estimación del \textit{Tempo} representó una excepción en el flujo de trabajo, siendo un escenario donde los extractores directos de librerías DSP (como Essentia) superaron significativamente el rendimiento del modelado predictivo estándar.

Un ensayo inicial utilizando un modelo de regresión con LightGBM —manteniendo el mismo diseño de variables predictivas que los modelos anteriores— demostró ser insuficiente. La evaluación de este modelo arrojó un Error Absoluto Medio (MAE) de 14,8394, un RMSE de 26,7187 y un coeficiente de determinación ($R^2$) prácticamente nulo de 0,0543. Como se evidencia en la figura \ref{fig:tempo_lgbm_rvp}, el modelo de regresión solo presenta capacidad predictiva alrededor de la moda (aproximadamente 120 BPM). Fuera de ese rango central, el algoritmo sobrestima los valores inferiores y subestima los superiores, fallando en capturar la relación con la variable objetivo.

\begin{figure}[htpb]
    \centering
    \includegraphics[width=0.7\textwidth]{tempo_lgbm_rvp.png}
    \caption{Dispersión de valores reales vs. estimados para \textit{Tempo} utilizando regresión con LightGBM. Se evidencia la incapacidad del modelo para generalizar fuera de la moda.}
    \label{fig:tempo_lgbm_rvp}
\end{figure}

Este bajo rendimiento no obedece a un déficit en la extracción de características, sino al fenómeno de multiplicidad descrito durante el preprocesamiento de los datos. La figura \ref{fig:tempo_multiple_relations} ilustra que la relación entre las etiquetas reales y los valores extraídos algorítmicamente no conforma una única línea recta continua, sino múltiples relaciones lineales superpuestas, generadas por la ambigüedad en la percepción de los compases y la confusión de octavas rítmicas (medios tiempos, dobles tiempos, etc.).

\begin{figure}[htpb]
    \centering
    \includegraphics[width=0.7\textwidth]{tempo_multiple_relations.png}
    \caption{Múltiples relaciones lineales y de proporción entre el tempo real (etiqueta) y el extraído por DSP.}
    \label{fig:tempo_multiple_relations}
\end{figure}

Ante la imposibilidad de que una regresión lineal mapee correctamente este ruido estructural en las etiquetas, se optó por analizar las predicciones directas de la librería de estado del arte Essentia. La figura \ref{fig:tempo_distributions} superpone la distribución del tempo real con el extraído (\texttt{tempo\_bpm}), evidenciando una similitud estadística sumamente alta entre ambas curvas.

\begin{figure}[htpb]
    \centering
    \includegraphics[width=0.7\textwidth]{tempo_distributions.png}
    \caption{Superposición de distribuciones: Tempo real vs. estimación directa de Essentia (\texttt{tempo\_bpm}).}
    \label{fig:tempo_distributions}
\end{figure}

Para validar esta elección, se analizó el comportamiento del error segmentado por las distintas proporciones métricas detectadas en el conjunto de datos. La tabla \ref{tab:tempo_relaciones_estadisticas} desglosa esta información, donde se aprecia que el 85,3\,\% de las pistas presentan una relación directa ($y \approx x$) con un error medio casi nulo de 0,0091 BPM.

\begin{table}[htpb]
    \centering
    \caption{Estadísticas de la estimación directa de Essentia según la relación proporcional con el tempo real.}
    \label{tab:tempo_relaciones_estadisticas}
    \renewcommand{\arraystretch}{1.2}
    \small
    \begin{tabular}{@{}lcccc@{}}
        \toprule
        \textbf{Relación ($y$ vs. $x$)} & \textbf{Proporción (\%)} & \textbf{Cantidad} & \textbf{Media de error} & \textbf{Desv. Est. ($\sigma$)} \\
        \midrule
        $y \approx x$       & 85,3 & 45.002 & 0,0091  & 2,1743 \\
        $y \approx 0,5x$    & 5,7  & 3.003  & -0,0734 & 3,3490 \\
        $y \approx 2x$      & 3,2  & 1.682  & -0,1027 & 2,5812 \\
        $y \approx 0,67x$   & 3,0  & 1.604  & 7,3190  & 6,9215 \\
        $y \approx 1,5x$    & 2,8  & 1.472  & -3,8125 & 8,2052 \\
        \bottomrule
    \end{tabular}
\end{table}

Las métricas finales de Essentia terminan de ratificar su fiabilidad. En una comparación directa estricta ($x = y$), se obtiene un MAE de 12,81 BPM y una exactitud del 80,3\,\% (dentro de un margen de tolerancia de $\pm5$ BPM). Sin embargo, al flexibilizar la evaluación para incorporar las relaciones armónicas rítmicas válidas (mitad, doble, etc.), el error absoluto medio se reduce a \textbf{1,45 ($\pm$4,02) BPM}, con un \textbf{92,4\,\%} de las pistas clasificadas correctamente dentro de la tolerancia. 

Estos resultados evidencian que el atributo DSP \texttt{tempo\_bpm} de Essentia captura la estructura rítmica real de manera mucho más precisa que cualquier intento de forzar una regresión sobre datos inherentemente ambiguos. Por tanto, se estableció prescindir de modelos predictivos adicionales y emplear directamente esta variable extraída como el resultado final para \textit{Tempo}.
%Se descartaron los estimadores tradicionales por errores de duplicación de pulsos. LightGBM resolvió efectivamente las ambigüedades multilineales mostradas en la figura \ref{fig:multi_linear_tempo}.

% \begin{figure}[htpb]
%     \centering
%     % \includegraphics[width=0.8\textwidth]{multi_linear_tempo.png} 
%     \caption{Relación multilineal del \textit{tempo} real y estimado.}
%     \label{fig:multi_linear_tempo}
% \end{figure}

\subsection{Speechiness y Liveness}

Tal como se evidenció en la evaluación del modelo multisalida preliminar, las variables \textit{Speechiness} y \textit{Liveness} presentaron un rendimiento deficiente cuando se intentaron predecir exclusivamente con representaciones DSP. El modelo inicial fue incapaz de generalizar los valores altos de estas distribuciones, obteniendo coeficientes de determinación ($R^2$) sumamente bajos (0,2796 y 0,1640, respectivamente). Este comportamiento motivó su separación analítica y la incorporación de los \textit{embeddings} y probabilidades de la red preentrenada PANNs para capturar el contexto semántico necesario.

Tras el preprocesamiento independiente y la optimización de hiperparámetros, se entrenaron modelos específicos para cada variable. La tabla \ref{tab:comparativa_speech_live} contrasta las métricas del experimento preliminar frente a los modelos definitivos, evidenciando un salto de rendimiento sustancial. En \textit{Speechiness}, el $R^2$ escaló a 0,6748, reduciendo el MAE a 0,0405. Por su parte, el modelo definitivo de \textit{Liveness} alcanzó un $R^2$ de 0,6318. 

\begin{table}[htpb]
    \centering
    \caption{Comparativa de métricas entre el modelo preliminar DSP y los modelos definitivos con PANNs para \textit{Speechiness} y \textit{Liveness}.}
    \label{tab:comparativa_speech_live}
    \renewcommand{\arraystretch}{1.2}
    \small
    \begin{tabular}{@{}lcccccc@{}}
        \toprule
        \multirow{2}{*}{\textbf{Variable}} & \multicolumn{2}{c}{\textbf{R² ($\uparrow$)}} & \multicolumn{2}{c}{\textbf{MAE ($\downarrow$)}} & \multicolumn{2}{c}{\textbf{RMSE ($\downarrow$)}} \\
        \cmidrule(lr){2-3} \cmidrule(lr){4-5} \cmidrule(lr){6-7}
        & \textbf{Inicial} & \textbf{Definitivo} & \textbf{Inicial} & \textbf{Definitivo} & \textbf{Inicial} & \textbf{Definitivo} \\
        \midrule
        \textit{Speechiness} & 0,2796 & \textbf{0,6748} & 0,0570 & \textbf{0,0405} & 0,1024 & \textbf{0,0675} \\
        \textit{Liveness}    & 0,1640 & \textbf{0,6318} & 0,0978 & \textbf{0,0811} & 0,1392 & \textbf{0,1301} \\
        \bottomrule
    \end{tabular}
\end{table}

La mejora predictiva se observa gráficamente en la figura \ref{fig:rvp_speech_live}. A diferencia del modelo inicial que colapsaba hacia la media, las estimaciones definitivas logran identificar correctamente los valores atípicos. En \textit{Speechiness} (izquierda), el modelo demuestra capacidad para detectar audios con alto contenido de voz hablada, como pistas de rap o intermedios vocales. En \textit{Liveness} (derecha), el algoritmo penaliza con valores bajos a las grabaciones de estudio puras y logra extender sus predicciones para reconocer las pistas capturadas en vivo.

\begin{figure}[htpb]
    \centering
    % Primera fila: Speechiness (Antes vs Después)
    \begin{subfigure}[b]{0.48\textwidth}
        \centering
        \includegraphics[width=\textwidth]{speechiness_antes_rvp.png}
        \caption{Speechiness: Preliminar (Solo DSP)}
        \label{fig:speech_before}
    \end{subfigure}
    \hfill
    \begin{subfigure}[b]{0.48\textwidth}
        \centering
        \includegraphics[width=\textwidth]{speechiness_despues_rvp.png}
        \caption{Speechiness: Definitivo (PANNs)}
        \label{fig:speech_after}
    \end{subfigure}

    \vspace{0.5cm} % Espacio entre filas

    % Segunda fila: Liveness (Antes vs Después)
    \begin{subfigure}[b]{0.48\textwidth}
        \centering
        \includegraphics[width=\textwidth]{liveness_antes_rvp.png}
        \caption{Liveness: Preliminar (Solo DSP)}
        \label{fig:live_before}
    \end{subfigure}
    \hfill
    \begin{subfigure}[b]{0.48\textwidth}
        \centering
        \includegraphics[width=\textwidth]{liveness_despues_rvp.png}
        \caption{Liveness: Definitivo (PANNs)}
        \label{fig:live_after}
    \end{subfigure}

    \caption{Comparativa de dispersión entre los modelos preliminares (izquierda) y definitivos (derecha) para \textit{Speechiness} y \textit{Liveness}. Se observa la transición del colapso hacia la media a una capacidad real de discriminación de valores altos.}
    \label{fig:rvp_speech_live_comparativa}
\end{figure}

El análisis de la importancia de características confirma el impacto absoluto del contexto semántico introducido por PANNs, operando en sinergia con el procesamiento de señales. 

Para \textit{Speechiness} (ver figura \ref{fig:f_imp_speechiness}), las variables de mayor peso son directamente las probabilidades de las clases 0000 (\textit{Speech}) y 0156 (\textit{Song}), demostrando que el modelo aprendió a discriminar eficientemente entre la voz hablada y el canto melódico. Asimismo, es notable la presencia de características dinámicas como los deltas de los coeficientes MFCC y el flujo espectral (\textit{Spectral flux}). Estas representaciones DSP son fundamentales porque capturan las transiciones rápidas, el ruido silábico y la naturaleza percusiva típica del fraseo en el rap o el habla, los cuales carecen de las vocales sostenidas del canto.

\begin{figure}[htpb]
    \centering
    \includegraphics[width=0.85\textwidth]{speechiness_fI.png}
    \caption{Top-15 variables predictivas para el modelo definitivo de \textit{Speechiness}.}
    \label{fig:f_imp_speechiness}
\end{figure}

Por otro lado, la predicción de \textit{Liveness} (ver figura \ref{fig:f_imp_liveness}) está monopolizada casi en su totalidad por la arquitectura PANNs. El modelo extrae su poder predictivo de los \textit{embeddings} latentes y, de manera explícita, de las desviaciones estándar y medias probabilísticas de las clases 0022 (\textit{Applause}), 0021 (\textit{Cheering}) y 0024 (\textit{Crowd}). Esto evidencia que la percepción de "música en vivo" para la máquina, al igual que para el oyente humano, no depende tanto de la acústica de la sala o de los instrumentos, sino de la detección algorítmica de la respuesta del público.

\begin{figure}[htpb]
    \centering
    \includegraphics[width=0.85\textwidth]{fi_livenes.png}
    \caption{Top-15 variables predictivas para el modelo definitivo de \textit{Liveness}.}
    \label{fig:f_imp_liveness}
\end{figure}
%Los experimentos sin \textit{embeddings} fracasaron al intentar capturar contexto. Para \textit{Speechiness} y \textit{Liveness}, las predicciones se acotaron al rango $[0, 1]$, logrando un RMSE de 0,0608 y 0,1352 respectivamente. En clasificación, el F1-Score macro fue de 0,2677 para \textit{Primary Mood} y 0,1595 para \textit{Secondary Mood}.

% \begin{figure}[htpb]
%     \centering
%     % \includegraphics[width=0.8\textwidth]{moods_confusion.png}
%     \caption{Matrices de confusión para \textit{Moods}.}
%     \label{fig:moods_confusion}
% \end{figure}

\subsection{Suggested Genres}

El problema de clasificar el género musical representó el desafío computacional y semántico más complejo del proyecto debido a la alta granularidad de las etiquetas. Se implementó un clasificador LightGBM (\textit{gbdt}) para distinguir entre 197 géneros y subgéneros. Al tratarse de una cantidad masiva de clases (muchas de ellas con una base rítmica o tímbrica similar), la evaluación priorizó la capacidad del modelo para generar un abanico de sugerencias coherentes de cara al usuario final.

Las métricas globales sobre el conjunto de prueba alcanzaron una exactitud (\textit{accuracy}) y un F1-Score (Macro y Ponderado) cercanos a 0,47. Si bien predecir el género exacto (\textit{Top-1}) arroja un 46,74\,\% de aciertos, el rendimiento del algoritmo escala drásticamente al analizar las estimaciones flexibles. La exactitud \textit{Top-3} alcanza un 68,32\,\%, mientras que la exactitud \textit{Top-5} asciende al 77,31\,\%. Esto significa que, en casi 8 de cada 10 casos, el modelo ubica el género correcto del audio entre sus primeras cinco sugerencias.

La figura \ref{fig:genre_confusion_matrix} presenta la matriz de confusión normalizada. Debido a la densidad de las 197 clases, la diagonal principal resalta la concentración de aciertos, pero también expone un alto nivel de dispersión fuera de la diagonal.

\begin{figure}[htpb]
    \centering
    \includegraphics[width=0.9\textwidth]{genre_ncm.png}
    \caption{Matriz de confusión normalizada para la clasificación de 197 géneros musicales.}
    \label{fig:genre_confusion_matrix}
\end{figure}

Un análisis cualitativo de las predicciones erróneas del modelo (\textit{Top-1}) demuestra que el algoritmo no fracasa de forma aleatoria, sino que sus confusiones poseen una fuerte coherencia acústica y de contexto. Al observar las estimaciones del \textit{Top-5}, se evidencia lo siguiente:
\begin{itemize}
    \item \textbf{Agrupación por familia rítmica:} Un audio etiquetado como \textit{Minimal House} fue clasificado erróneamente como \textit{Funk House}, pero el \textit{Top-5} estuvo compuesto exclusivamente por subgéneros de la música electrónica bailable (\textit{Contemporary House}, \textit{Tech House}, \textit{Chicago House}). 
    \item \textbf{Cruce de estéticas vocales y armónicas:} Una pista de \textit{Trap Soul} se confundió con \textit{Neo-Soul} y \textit{Contemporary R\&B}. De igual manera, un audio de \textit{Alternative Based R\&B/Soul} fue asignado a \textit{Downtempo} y \textit{Neo-Soul}. El modelo detecta correctamente los \textit{grooves} lentos, los bajos profundos y los arreglos vocales sofisticados, confundiendo etiquetas que los propios críticos y curadores humanos suelen debatir.
    \item \textbf{Similitud de instrumentación:} Una pista de \textit{Latin Traditional} fue catalogada como \textit{Jazz Vocals}. Es altamente probable que la presencia de ensambles acústicos, metales e improvisación vocal haya inducido al modelo a priorizar la textura general del ensamble por encima del ritmo folclórico específico.
\end{itemize}

Finalmente, la importancia de las características predictivas, expuesta en la figura \ref{fig:genre_feature_importance}, revela que el modelo no se apoya en características semánticas de alto nivel (como los \textit{embeddings}), sino en el análisis físico fundamental del sonido.

\begin{figure}[htpb]
    \centering
    \includegraphics[width=0.85\textwidth]{fi_genre.png}
    \caption{Top-15 variables predictivas del modelo LightGBM para la sugerencia de género (\textit{Suggested Genre}).}
    \label{fig:genre_feature_importance}
\end{figure}

Para discriminar entre 197 géneros, el algoritmo prioriza fuertemente la intensidad (LUFS y RMS) y las características macro del espectro: la presencia de transientes rítmicos (\textit{Crest factor} y \textit{Spectral flux}), el brillo (\textit{Spectral entropy}) y el contraste espectral, el cual resulta vital para diferenciar arreglos instrumentales densos (como una orquesta de salsa) de géneros minimalistas (como el \textit{Lo-Fi} o el \textit{Minimal House}).


\section{Validación productiva}
\label{sec:validacion_produccion}

%El cliente CLI \textit{Beethoven} integró la extracción y predicción (\textit{Zahir API}) sobre audios inéditos en OCI, persistiendo los metadatos JSON en la base de datos de forma estable y asíncrona.

% \begin{figure}[htpb]
%     \centering
%     % \includegraphics[width=0.95\textwidth]{ui_production_log.png}
%     \caption{Proceso de inferencia productivo.}
%     \label{fig:produccion_ejemplo}
% \end{figure}

%todo Dada la complejidad de objetivos como los géneros musicales, 197 clases resultantes, se priorizó el análisis de la precisión \textit{Top-K} para capturar la pluralidad semántica de la música
%----------------------------------------------------------------------------------------
%	SECTION 1
%----------------------------------------------------------------------------------------

% todo this ...
% Un ejemplo de esta relación se muestra en la figura \ref{fig:multi_linear_tempo}, donde el \textit{tempo} real etiquetado muestra múltiples relaciones lineales con la variable predictiva \textit{tempo} estimada desde Essentia.  
% \begin{figure}[htpb]
%     \centering
%     \includegraphics[width=0.8\textwidth]{multi_linear_tempo.png} 
%     \caption[Múltiples relaciones lineales de tempo real y estimado]{Relación multilineal del \textit{tempo} real y el tempo global estimado de Essentia.}
%     \label{fig:multi_linear_tempo}
% \end{figure}

% \section{Pruebas funcionales del hardware}
% \label{sec:pruebasHW}

% La idea de esta sección es explicar cómo se hicieron los ensayos, qué resultados se obtuvieron y analizarlos.
